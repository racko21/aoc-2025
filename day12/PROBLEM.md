# Day 12: Christmas Tree Farm

Given a list of polyomino shapes and a list of regions (W×H grids), determine how many regions can fit all required quantities of each shape without any '#' cells overlapping. Shapes can be rotated and flipped. '.' cells in a shape are transparent (non-blocking).
